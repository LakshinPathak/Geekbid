# Claude Code Prompt — Talent Pool & Client Interaction Flow Audit

Act as a regular client user on the GeekBid platform. Trace the complete interaction flow on the Talent Pool page and every freelancer-facing action a client can take. For each button — Message, Invite, Hire, View Profile, Filter/Search — follow the code path end-to-end: what the button's onClick does → which API route it hits → what DB operation runs → what response comes back → what the UI shows the user. Check: Do dead buttons exist (onClick missing/empty)? Do any API calls fail silently without user feedback? Are error states handled (toast/modal)? Does the Invite flow actually create an invite record? Does Hire trigger a job/offer creation? Does Message open a real chat or just a stub? Also check the freelancer profile page (`/profile/[id]`) for the same buttons. Output a flow-by-flow verdict table: Button → Frontend handler → API route → DB effect → Success UX → Error UX → PASS/FAIL with specific evidence (file:line).
